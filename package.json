{
  "name": "shoppingonline-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd server && npm install --no-audit --no-fund",
    "start": "cd server && node index.js"
  }
}